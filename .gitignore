# Python tooling outputs
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
*.egg-info/
build/
.venv/
.python-version

# Site + release outputs (generated, never tracked)
/dist/
/dist-release/
/dist-preview/
/site-payloads/
instances_v2/

# Persistent publication state (history ledger + snapshot inventories)
/publish-state/

# Local working notes (campaign plans, design records): not tracked, not published
/docs/reports/

# MkDocs (the documentation is built into dist/docs/ by `site build`;
# a stray `mkdocs build` without config and the Material plugin cache)
/site/
/.cache/
/reports/

# Article folder
/MAMUT_routing_paper/